version: 1
send_anonymous_usage_stats: true

plugins:
  extractors:
    - name: tap-constant
      namespace: tap_constant
      pip_url: git+https://github.com/thanhcang/meltano-tap-constant.git
      executable: tap-constant
      capabilities:
        - state
        - catalog
        - discover
        - about
        - stream-maps